version: '2'
services:
  db:
    image: 'postgres:latest'
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: sociality
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres